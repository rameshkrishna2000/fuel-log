import { autoPlannerURLs } from '../features/autoplanner/utils/autoPlannerURLs';

import { commonURLS } from '../common/utils/commonURLs';

const URLs = {
  ...commonURLS,

  ...autoPlannerURLs
};

export default URLs;
